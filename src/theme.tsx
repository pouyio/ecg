import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002735",
    },
    secondary: {
      main: "#19857b",
    },
    background: {
      default: "#f2f2f2",
    },
  },
});

export default theme;
